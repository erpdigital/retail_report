# Purchase Receipt Scanner - baseline

Verbatim dump of the DocTypes and Client Script as they existed **only in the
`dev.erpnize.de` database** (`custom=1`, module `Stock`, `developer_mode=0`),
captured before the rewrite to a Vue page.

Nothing here is loaded by the app. It is a restore point, not source.
